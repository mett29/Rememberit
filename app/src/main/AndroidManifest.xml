<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.mattiamancassola.rememberit" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/MyTheme"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name="com.app.mattiamancassola.rememberit.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/MyTheme"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.app.mattiamancassola.rememberit.SettingsActivity"
            android:label="Info | Record"
            android:parentActivityName="com.app.mattiamancassola.rememberit.MainActivity"
            android:theme="@style/MyTheme"
            android:screenOrientation="portrait" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.app.mattiamancassola.rememberit.MainActivity" />
        </activity>

    </application>

</manifest>
